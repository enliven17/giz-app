# mock: existing investment demo; probe: isolated P1 native compatibility screen.
# Native operations inside the probe fail closed until real identities are supplied.
# native: reserved for future integrated access, currently blocked.
# Public configuration only; never add key material or secrets here.
EXPO_PUBLIC_PASSKEY_MODE=mock
