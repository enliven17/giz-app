# mock: existing investment demo.
# native-probe: retired; rejected because the old signer is disconnected.
# native: blocked until the native signer is implemented.
# The former JavaScript probe mode was removed and is rejected.
# Public configuration only; never add key material or secrets here.
EXPO_PUBLIC_PASSKEY_MODE=mock
